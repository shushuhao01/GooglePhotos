# 当前已完成开发备份

备份时间：2026-09-04

## 完整项目副本

当前 39 个项目文件已完整复制到：

`/Users/huangfeng/google image/web-image-uploader-backup-2026-09-04`

该副本包含源码、Manifest、图标、测试、脚本、README 和全部文档。已校验源目录与备份目录文件数均为 39，且 `manifest.json`、`src/popup/popup.js` 哈希一致。后续收费模式开发如需回看基线，可直接打开该目录；不要在备份目录上继续开发。

同时生成压缩备份：`/Users/huangfeng/google image/web-image-uploader-backup-2026-09-04.zip`。

## 已完成功能

- 网页图片扫描：img、srcset、picture、懒加载属性、CSS 背景和图片链接。
- 内容图默认筛选，头像/图标/广告/表情等装饰识别。
- 列表/卡片视图、标准/完整比例缩略图、勾选和批量操作。
- 图片点击预览、左右切换、自动播放、真正浏览器全屏预览。
- 图片任务与历史记录缩略图。
- Google OAuth 持久登录、401 刷新和授权失效处理。
- Google Photos 批量上传、相册创建、指定相册选择和并发写入串行化。
- 普通下载、选择目录下载、ZIP 打包按钮及候选 URL/缩略图回退。
- 自动新建相册可自定义名称，留空使用模板；默认目标为“放相册库”。
- 图标资源已使用项目 logo 生成 16/32/48/128 PNG。
- Manifest、README 和设置页已更新为“网页图片上传器 · Web Image Uploader”。

## 当前已知边界

- 某些网站允许浏览器显示图片，但禁止扩展读取响应二进制；普通下载可成功，扩展内 ZIP 可能失败。收费方案要求的服务端中转尚未接入。
- Google Photos OAuth 正式商店发布需要使用正式扩展 ID、Production Consent Screen 和 Google 审核。
- 当前项目尚无真实支付商户号、产品账号服务端和管理后台，不能宣称已支持真实收费。

## 验证记录

- `npm test`：28 项通过。
- `npm run check`：18 个 JS 文件和 Manifest 检查通过。

该文件用于在收费模式开发前固定当前基线；后续变更应继续追加日期、版本和验收结果。
